# Project Status

This repository is being prepared as a public-facing R Shiny project for ARG hazard-score lookup and abundance-informed sample indexing.

Current setup status:

- Public repository created.
- Private score database should not be committed.
- `.gitignore` should exclude private score tables, master Excel workbooks, validation files, and deployment credentials.
- The app should show only user query results, uploaded dataset outputs, templates, examples, and documentation.

Next development step: add the Shiny app structure, helper scripts, public examples, and documentation files.
